CREATE TABLE Movies
(
    id         int       PRIMARY KEY,
    title      text      NOT NULL,
    year       int       NOT NULL,
    director   int       NOT NULL,
    length     text      NOT NULL,
    rating     int       NOT NULL
);